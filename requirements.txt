# --- Core ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
httpx>=0.27.0

# --- LLM ---
# anthropic>=0.39.0

# --- Orchestration ---
langgraph>=0.2.50
langchain>=0.3.0           # base abstractions (used by langgraph)
# langchain-anthropic>=0.2.0 # optional, only if you swap to LangChain models
langchain_groq
langchain-huggingface 
huggingface_hub

# --- Observability ---
langfuse>=2.50.0
langsmith>=0.1.140         # auto-activated via LANGCHAIN_API_KEY

# --- Guardrails (optional heavy library — see docs/guardrails.md) ---
# guardrails-ai>=0.5.0
# presidio-analyzer>=2.2.0   # for production-grade PII detection
# presidio-anonymizer>=2.2.0

# --- Multi-agent alternative (optional) ---
# crewai>=0.80.0

# --- Dev / test ---
pytest>=8.3.0
pytest-asyncio>=0.24.0
black>=24.10.0
ruff>=0.7.0
mypy>=1.13.0
