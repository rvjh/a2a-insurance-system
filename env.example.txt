# Copy to .env and fill in your values. NEVER commit .env.

# --- Anthropic LLM (REQUIRED) ---
ANTHROPIC_API_KEY=sk-ant-...

# --- Service URLs (defaults work for local dev) ---
GATEWAY_HOST=0.0.0.0
GATEWAY_PORT=8000
CLAIMS_AGENT_URL=http://localhost:8001
POLICY_AGENT_URL=http://localhost:8002

# --- Auth ---
# Bearer token clients send to the gateway. Generate a strong one for prod.
API_BEARER_TOKEN=dev-local-token-change-me

# --- Observability (all OPTIONAL — system works without them) ---
# Langfuse: https://cloud.langfuse.com — sign up, create a project, copy keys
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com

# LangSmith: https://smith.langchain.com — create an API key
LANGCHAIN_API_KEY=
LANGCHAIN_PROJECT=a2a-insurance-system

# --- Behavior ---
LOG_LEVEL=INFO
DEFAULT_MODEL=claude-sonnet-4-5
REQUEST_TIMEOUT_S=30
